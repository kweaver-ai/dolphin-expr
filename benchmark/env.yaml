# Benchmark Environment Configuration
# 存放一些基本的配置信息，比如数据库的配置信息等

# 数据库配置
database:
  host: localhost
  port: 5432
  name: benchmark_db
  user: benchmark_user
  password: benchmark_pass

# 其他通用配置
general:
  max_workers: 4
  retry_count: 3
  log_level: INFO
