$_root_dir + "/data/rule.md" -> rule_path
$_root_dir + "/data/table.md" -> table_path
$_root_dir + "/data/default_WHERE_clause.txt" -> default_clause_path

@_read_file(file_path=$rule_path) -> rule
@_read_file(file_path=$table_path) -> table
@_read_file(file_path=$default_clause_path) -> default_clause


/prompt/现在要找到 query 应该遵循的 where clause 要求

【where clause】
```
$default_clause
```

【问题】
```
$query
```

请先仔细思考问题，然后返回这个 query 应该遵循的 where clause 要求(完整保留，包括注释和解释)
请直接返回结果，现在开始 -> where_clause

/prompt/现在要从一些业务知识中找到对某个问题的解决必要的部分

【业务规则】
```
$rule
```

【问题】
```
$query
```

【注意】
    - 请完成思考，然后【完整】的输出【相关】的业务规则内容，不相关的业务规则不要给出

请不要返回别的内容，直接返回结果，现在开始 -> key_rules


/explore/(tools=$tools) 你是一位资深的Oracle数据库专家，拥有超过10年的Oracle SQL开发和优化经验。你精通Oracle语法规范、性能优化、复杂查询构建，并且以严谨、细致、一丝不苟的工作态度著称。
现在要解决一个重要的问题

## 📋 Core Inputs

### [Schema Information]
$table

### [WHERE Clause Requirements - Must Remember Completely]
$where_clause.answer

### [WHERE Clause Reference Template]
$default_clause

⚠️ **关键警告**：务必牢记要【完整】的满足 SQL WHERE CLAUSE 的要求，不要出现遗漏！！

### [Business Rules - Must Be Strictly Followed]
$key_rules.answer

### [Database]
$db_id

### [Question]
$query

## 🧠 **Key Reminders and Notes**

### Key points to focus on:
- **WHERE clause requirements**: Core conditions that must be reflected in the SQL.
- **Business rules**: Rules that determine the SQL logic and structure.

### Oracle SQL formatting requirements:
- 表名需要schema前缀，如"WATSONS.DWS_ORDER_ITEM_STAFF_ORDER_BY_DAY"
- SQL语句结尾不加分号

### Execution suggestions:
根据问题复杂度采用不同策略：

**简单问题**：
1. 直接分析题目要求，生成SQL语句
2. 使用工具执行验证SQL的正确性
3. 根据验证结果调整答案

**复杂问题**：
1. 使用拆分工具将问题分解为多个子问题
2. 针对每个子问题进行分析和推理
3. 边拆分边使用工具验证中间结果
4. 通过多个工具交叉验证，确保逻辑正确性
5. 整合所有验证过的子结果，形成最终答案

**核心原则**：
- 利用工具进行**交叉验证**，而非单纯拆分
- 复杂问题采用"拆分→推理→验证→再拆分→再推理→再验证"的循环模式
- **禁止未经工具验证直接给出答案**
- 确保每个推理步骤都有工具验证支撑


**开始分析** -> model_answer

/prompt/(system_prompt="test") 根据生成的答案，返回选择题的正确答案选项
格式：```A``` 或 ```A,B,C```（多选题）

生成的答案: $model_answer.answer
问题和选择题答案：$choice_question

请给出结果 -> final_result
